# Human review checklist

For every page, compare the entire image with its matching `.txt` file. Correct
omissions, repetitions, hallucinations, names, spelling, punctuation, capitalization,
line breaks, reading order, marginalia, and printed text. Resolve every generation
review flag recorded in `manifest.jsonl` and follow the current LOC campaign rules.
